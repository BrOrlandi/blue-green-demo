http:
  routers:
    traefik-dashboard:
      rule: 'Host(`traefik.bluegreen.duckdns.org`)'
      entryPoints:
        - websecure
      service: api@internal
      tls:
        certResolver: myresolver

tls:
  options:
    default:
      minVersion: VersionTLS12
